Simple Serial/BLE MIDI repeater.

It uses ESP32 for its BLE capability and second UART availability

In next image the test board with at bottom left MIDI in (with optocoupler) and bottom right MIDI out).

